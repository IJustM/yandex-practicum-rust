# Проектная работа

Егоров Дмитрий

## cli

### converter

Пример запуска `cargo run --bin converter -- --from <path.ext> --to <path.ext>`

### comparer

Пример запуска `cargo run --bin comparer -- --file1 <path.ext> --file2 <path.ext>`
